To start the React native project

npm install
npx react-native run-android OR npx react-native run-ins

how to apply other Url in native?

function openWebsite(websiteLink: string){
    Linking.openURL(websiteLink)
  }

